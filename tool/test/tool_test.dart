import 'package:tool/tool.dart';
import 'package:test/test.dart';

void main() {
  group('generateGradientSamples', () {
    test('should generate gradient samples', () {
       generateGradientSamples();
    }, skip: 'Skipping till the memory used in the function has been mocked.');
  });

}
